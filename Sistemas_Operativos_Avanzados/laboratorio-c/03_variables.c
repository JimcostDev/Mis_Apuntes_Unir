#include <stdio.h>

// declaracion de variables
unsigned short edad = 0;
int a, b, c;

int main(){
  // inicializacion de variables
  a = 1;
  b = 34;
  c = a + b;
  printf("La suma es %d:", c);
  return 0;
}